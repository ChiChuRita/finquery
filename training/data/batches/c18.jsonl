{"id": "c18-shipped-en-001", "household": "shipped", "language": "en", "request": "Draw two lines, January to June of 2025 and July to December, so I can see which run was dearer.", "shape": "line", "difficulty": 3, "plan": {"title": "First half against second half", "question": "spending of 2025 per half of the year and per month inside that half, one row per half and month, half named 'First half' or 'Second half', month_of_half counted 1 to 6, columns month_of_half, half and total_eur", "columns": ["month_of_half", "half", "total_eur"]}, "sql": "SELECT CAST(strftime('%m', booked_on) AS INTEGER) - CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 0 ELSE 6 END AS month_of_half,\n       CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'First half' ELSE 'Second half' END AS half,\n       ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month_of_half, half\nORDER BY half, month_of_half", "code": "const amounts = data.map((row) => row.total_eur);\nreturn defineChart({\n  marks: [\n    lineY(data, { x: 'month_of_half', y: 'total_eur', z: 'half', color: 'half', strokeWidth: 2, points: true }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.06), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: {\n      scale: scaleLinear().domain([Math.min(0, ...amounts), Math.max(0, ...amounts)]),\n      nice: true,\n      grid: true,\n      axis: { ticks: { format: eurShort } },\n    },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.half + ' ' + point.datum.month_of_half + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\nit compares two halves of one year over the months inside them\na line per half reads the two runs against each other\nthe axis is the month inside the half, 1 to 6, so both lines start together\nthe columns are month_of_half, half and total_eur, the euro figure last", "reasoning_code": "the shape is line and these rows carry a series, so one lineY over all of them\nthe columns are month_of_half, half and total_eur, and total_eur holds the numbers\nhalf names the series, so it is the z and the color channel and the legend goes on\nmonth_of_half is a name and not a month, so no monthShort and no label dropped\na line brings no baseline, so the euro domain is named over every figure, zero included"}
{"id": "c18-shipped-de-002", "household": "shipped", "language": "de", "request": "Stell die Ausgaben aus dem dritten und dem vierten Quartal 2025 je Kategorie nebeneinander.", "shape": "bar_grouped", "difficulty": 3, "plan": {"title": "Drittes gegen viertes Quartal", "question": "Ausgaben des dritten und des vierten Quartals 2025 je Kategorie, eine Zeile je Kategorie und Zeitraum, der Zeitraum heisst 'Drittes Quartal' oder 'Viertes Quartal', sechs Kategorien, Spalten topic, period und total_eur", "columns": ["topic", "period", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic,\n       CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 9 THEN 'Drittes Quartal' ELSE 'Viertes Quartal' END AS period,\n       ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\n  AND booked_on BETWEEN '2025-07-01' AND '2025-12-31'\n  AND category IN ('Groceries', 'Transport', 'Dining', 'Shopping', 'Subscriptions', 'Health')\nGROUP BY topic, period\nORDER BY topic, period", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'topic', y: 'total_eur', z: 'period', color: (row) => short(row.period), layout: group({ padding: 0.12 }), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.2), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.period + ' ' + point.datum.topic + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nzwei Quartale werden je Kategorie verglichen, das sind zwei Dimensionen, die sich kreuzen\ngruppierte Balken stellen die beiden Quartale je Kategorie nebeneinander\nder Zeitraum ist die Serie, die Kategorie die Position, also lange Zeilen\ndie Spalten sind topic, period und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar_grouped, so barY carries a series and layout: group()\nthe columns are topic, period and total_eur, and total_eur holds the numbers\nperiod is the series, so it is the z and the color channel and it needs a legend\ncategory names sit on x, so no label is dropped and they are tilted\neuros on y from the bare factory, because bars rest on zero themselves"}
{"id": "c18-shipped-en-003", "household": "shipped", "language": "en", "request": "Quarter by quarter in 2025, plot the swing up or down against the quarter before it.", "shape": "bar", "difficulty": 3, "plan": {"title": "Change to the quarter before", "question": "for each quarter of 2025 after the first the difference between what was spent in it and what was spent the quarter before, signed so that a cheaper quarter is negative, one row per quarter, columns quarter as '2025-Q1' and change_eur", "columns": ["quarter", "change_eur"]}, "sql": "SELECT quarter, change_eur\nFROM (\n  SELECT '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter,\n         ROUND(-SUM(amount) - LAG(-SUM(amount)) OVER (ORDER BY '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3)), 2) AS change_eur\n  FROM transaction_view\n  WHERE amount < 0\n  GROUP BY quarter\n)\nWHERE change_eur IS NOT NULL\nORDER BY quarter", "code": "return defineChart({\n  marks: [\n    barY(data, { x: 'quarter', y: 'change_eur', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.26), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => point.datum.quarter + ': ' + eur(point.datum.change_eur) },\n});", "reasoning_plan": "the request is written in English, so language en\nit compares every quarter with the one before it, so one figure per quarter\nbars rest on zero, so a cheaper quarter is drawn below the baseline\nthe figure keeps its sign and is never two positive columns\nthe columns are quarter and change_eur, the euro figure last", "reasoning_code": "the shape is bar, so the mark is barY\nthe columns are quarter and change_eur, and change_eur holds the numbers\na quarter reads 2025-Q2, which is a name and not a month, so no monthShort and no label dropped\na bar rests on zero, so its euro axis is the bare scaleLinear factory and the negative bars hang below it"}
{"id": "c18-shipped-de-004", "household": "shipped", "language": "de", "request": "Wie haben sich meine Ausgaben im vierten Quartal 2025 auf die Kategorien verteilt?", "shape": "doughnut", "difficulty": 2, "plan": {"title": "Ausgaben im vierten Quartal nach Kategorie", "question": "die sechs groessten Ausgabenkategorien im vierten Quartal 2025 mit ihren Summen, eine Zeile je Kategorie, Spalten topic und total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-10-01' AND '2025-12-31'\nGROUP BY topic\nORDER BY total_eur DESC\nLIMIT 6", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nconst slices = pie(data, { value: 'total_eur' });\nreturn defineChart({\n  marks: [\n    polar({\n      inset: 6,\n      radiusRatio: 0.92,\n      marks: [\n        radialArc(slices, {\n          innerRadius: ({ radius }) => radius * 0.62,\n          color: (slice) => short(slice.topic),\n          key: 'topic',\n        }),\n      ],\n      scales: { angle: null, radius: null },\n    }),\n  ],\n  scales: { x: null, y: null },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nes wird gefragt, wie sich ein Ganzes aufteilt, das ist ein Anteil\nein Doughnut zeigt Anteile am direktesten\nder Zeitraum ist das vierte Quartal 2025, die Kuebel sind die eigenen Kategorien\ndie Spalten sind topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is doughnut, so pie allocates the slices and radialArc draws them\nthe columns are topic and total_eur, and total_eur is the value pie reads\nthe slices are the series, so color and key both name topic, the column of names\na doughnut has no axes, so scales.x and scales.y are null and the legend is on"}
{"id": "c18-shipped-en-005", "household": "shipped", "language": "en", "request": "Draw the running total of each half of 2025 as two bands over the six months inside it.", "shape": "area", "difficulty": 3, "plan": {"title": "Spending added up, half against half", "question": "spending of 2025 added up inside each half of the year, one row per half and per month of that half, the running total per half, columns month_of_half as 1 to 6, half as 'First half' or 'Second half' and cumulative_eur", "columns": ["month_of_half", "half", "cumulative_eur"]}, "sql": "SELECT CAST(strftime('%m', booked_on) AS INTEGER) - CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 0 ELSE 6 END AS month_of_half,\n       CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'First half' ELSE 'Second half' END AS half,\n       ROUND(SUM(-SUM(amount)) OVER (PARTITION BY CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'First half' ELSE 'Second half' END ORDER BY CAST(strftime('%m', booked_on) AS INTEGER)), 2) AS cumulative_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month_of_half, half\nORDER BY half, month_of_half", "code": "return defineChart({\n  marks: [\n    areaY(data, { x: 'month_of_half', y1: 0, y2: 'cumulative_eur', z: 'half', color: 'half', fillOpacity: 0.18 }),\n    lineY(data, { x: 'month_of_half', y: 'cumulative_eur', z: 'half', color: 'half', strokeWidth: 2 }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.02), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.half + ' ' + point.datum.month_of_half + ': ' + eur(point.datum.cumulative_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\n'added up' is a running total, so an area, and 'compare' puts a second one beside it\nthe half is the series, so one band per half and a legend\nthe axis is the month inside the half, 1 to 6, so both bands start at the same place\nthe columns are month_of_half, half and cumulative_eur, the euro figure last", "reasoning_code": "the shape is area and these rows carry a series, so one areaY and one lineY over all of them\nthe columns are month_of_half, half and cumulative_eur, and cumulative_eur holds the numbers\nhalf names the series, so it is the z and the color channel on both marks and the legend goes on\nthe two bands are compared and not added, so each rests on zero itself: y1 zero, y2 the figure\nan area rests on zero, so its euro axis is the bare scaleLinear factory"}
{"id": "c18-shipped-de-006", "household": "shipped", "language": "de", "request": "Zeig mir pro Quartal 2025 gestapelt, wie sich Lebensmittel, Transport, Essengehen und Wohnen verteilen.", "shape": "bar_stacked", "difficulty": 2, "plan": {"title": "Vier Kategorien je Quartal", "question": "Ausgaben fuer die Kategorien Groceries, Transport, Dining und Housing je Quartal 2025, eine Zeile je Quartal und Kategorie, Spalten quarter als '2025-Q1', topic und total_eur", "columns": ["quarter", "topic", "total_eur"]}, "sql": "SELECT '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter, category AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND category IN ('Groceries', 'Transport', 'Dining', 'Housing')\nGROUP BY quarter, topic\nORDER BY quarter, topic", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'quarter', y: 'total_eur', z: 'topic', color: (row) => short(row.topic), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.22), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + point.datum.quarter + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nvier Kategorien werden ueber vier Quartale verglichen, zwei Dimensionen, die sich kreuzen\ngestapelte Balken zeigen die Verteilung und die Quartalssumme zugleich\ndie Anfrage nennt Quartale, also steht das Quartal auf der Achse und nie ein Monat\ndie Spalten sind quarter, topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar_stacked, so barY carries a series and no layout\nthe columns are quarter, topic and total_eur, and total_eur holds the numbers\ntopic is the series, so it is the z and the color channel and it needs a legend\na quarter is a name and not a month, so no monthShort and no label dropped\na stack is taller than any single row, so the euro axis names no domain"}
{"id": "c18-shipped-en-007", "household": "shipped", "language": "en", "request": "Which categories did I spend more on this month than last month? Rank them by the difference.", "shape": "bar_horizontal", "difficulty": 3, "plan": {"title": "Categories by change to last month", "question": "for each spending category the difference between what was spent in December 2025 and what was spent in November 2025, signed so that a cheaper category is negative, ordered by that difference, one row per category, columns topic and change_eur", "columns": ["topic", "change_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic,\n       ROUND(SUM(CASE WHEN strftime('%Y-%m', booked_on) = '2025-12' THEN -amount ELSE 0 END)\n             - SUM(CASE WHEN strftime('%Y-%m', booked_on) = '2025-11' THEN -amount ELSE 0 END), 2) AS change_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-11-01' AND '2025-12-31'\nGROUP BY topic\nORDER BY change_eur DESC", "code": "return defineChart({\n  marks: [\n    barX(data, { x: 'change_eur', y: 'topic', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n    y: { scale: () => scaleBand().padding(0.22), axis: { tickLabels: { thin: false } } },\n  },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.change_eur) },\n});", "reasoning_plan": "the request is written in English, so language en\nit ranks categories by how much they moved between two months\nlong category names read best lying down, so horizontal bars\nthe difference keeps its sign, so a cheaper category points left of zero\nthe columns are topic and change_eur, the euro figure last", "reasoning_code": "the shape is bar_horizontal, so the mark is barX and nothing else\nthe columns are topic and change_eur, and change_eur holds the numbers\nbarX draws sideways, so the euro column is x and the names are y\nscales.x is the euro axis from the bare factory, scales.y the band of names, none dropped"}
{"id": "c18-shipped-de-008", "household": "shipped", "language": "de", "request": "Zeig als Sankey, wohin mein Geld im vierten Quartal 2025 geflossen ist.", "shape": "sankey", "difficulty": 3, "plan": {"title": "Viertes Quartal in die Kategorien", "question": "Ausgaben des vierten Quartals 2025 je Kategorie als Fluss, eine Zeile je Kategorie, die Quelle heisst fuer jede Zeile 'Viertes Quartal' und die Kategorie ist das Ziel, jeder Betrag positiv, Quelle und Ziel sind nie derselbe Name und keine Summenzeile, Spalten source, target und amount_eur", "columns": ["source", "target", "amount_eur"]}, "sql": "SELECT 'Viertes Quartal' AS source, coalesce(category, 'Needs review') AS target, ROUND(-SUM(amount), 2) AS amount_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-10-01' AND '2025-12-31'\n  AND category IN ('Groceries', 'Transport', 'Cash', 'Dining', 'Shopping')\nGROUP BY source, target\nORDER BY amount_eur DESC", "code": "const names = [];\ndata.forEach((row) => {\n  if (names.indexOf(row.source) === -1) names.push(row.source);\n  if (names.indexOf(row.target) === -1) names.push(row.target);\n});\nconst nodes = names.map((name) => ({ id: name }));\nreturn defineChart({\n  marks: [\n    sankeyDiagram({\n      nodes: nodes,\n      links: data,\n      nodeKey: 'id',\n      source: 'source',\n      target: 'target',\n      value: 'amount_eur',\n      align: 'justify',\n      nodeWidth: 14,\n      nodePadding: 8,\n      inset: 8,\n      marks: (context) => [\n        link(context.links, {\n          x1: 'x1',\n          y1: 'y1',\n          x2: 'x2',\n          y2: 'y2',\n          key: 'key',\n          stroke: palette[1],\n          strokeOpacity: 0.3,\n          strokeWidth: (flow) => Math.max(1, flow.width),\n          lineCap: 'butt',\n        }),\n        rect(context.nodes, { x1: 'x0', x2: 'x1', y1: 'y0', y2: 'y1', key: 'key', fill: palette[0], inset: 0 }),\n        text(context.nodes, {\n          x: 'x',\n          y: 'y',\n          key: 'key',\n          text: (node) => node.data.id,\n          dx: (node) => (node.depth === 0 ? 12 : -12),\n          anchor: (node) => (node.depth === 0 ? 'start' : 'end'),\n          fontSize: 11,\n        }),\n      ],\n    }),\n  ],\n  scales: { x: null, y: null },\n  guides: false,\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.source + ' nach ' + point.datum.target + ': ' + eur(point.datum.amount_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nein Quartal fliesst in seine Kategorien, das ist ein Fluss von einer Quelle zu Zielen\nein Sankey zeigt, wohin das Geld des Quartals geht\njede Zeile ist ein Fluss mit positivem Betrag, Quelle und Ziel verschieden\ndie Spalten sind source, target und amount_eur", "reasoning_code": "the shape is sankey, so sankeyDiagram lays the flow out\nthe columns are source, target and amount_eur, and amount_eur holds the numbers\nthe nodes are the names of both ends, collected from the rows themselves\nthe child marks read the layout's own fields (x0, x1, y0, y1, key), never a column of the query"}
{"id": "c18-shipped-en-009", "household": "shipped", "language": "en", "request": "Show what I spent in each quarter of 2025.", "shape": "line", "difficulty": 1, "plan": {"title": "Spending per quarter", "question": "total spending per quarter of 2025, one row per quarter, ordered, columns quarter as '2025-Q1' and total_eur", "columns": ["quarter", "total_eur"]}, "sql": "SELECT '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY quarter\nORDER BY quarter", "code": "const amounts = data.map((row) => row.total_eur);\nreturn defineChart({\n  marks: [\n    lineY(data, { x: 'quarter', y: 'total_eur', stroke: palette[0], strokeWidth: 2.25, points: true }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.06), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: {\n      scale: scaleLinear().domain([Math.min(0, ...amounts), Math.max(0, ...amounts)]),\n      nice: true,\n      grid: true,\n      axis: { ticks: { format: eurShort } },\n    },\n  },\n  tooltip: { use: tooltip, format: (point) => point.datum.quarter + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "the request is written in English, so language en\nit compares the four quarters of one year in order\na line reads an ordered run of periods most directly\nthe request says quarters, so the axis carries quarters and never months\nthe columns are quarter and total_eur, the euro figure last", "reasoning_code": "the shape is line, so the mark is lineY over the rows\nthe columns are quarter and total_eur, and total_eur holds the numbers\na quarter reads 2025-Q1, which is a name and not a month, so no monthShort and no label dropped\na line brings no baseline of its own, so its euro axis names the domain over the amounts"}
{"id": "c18-shipped-de-010", "household": "shipped", "language": "de", "request": "Balken je Monat 2025 fuer Lebensmittel, und zieh den Schnitt quer darueber.", "shape": "bar", "difficulty": 3, "plan": {"title": "Lebensmittel je Monat mit Durchschnitt", "question": "Ausgaben der Kategorie Groceries pro Monat 2025, eine Zeile je Monat, geordnet, Spalten month als 'YYYY-MM' und total_eur", "columns": ["month", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND category = 'Groceries'\nGROUP BY month\nORDER BY month", "code": "return defineChart({\n  marks: [\n    barY(data, { x: 'month', y: 'total_eur', fill: palette[0], maxThickness: 32 }),\n    ruleY([mean(data, 'total_eur')]),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.26), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\njeder Lebensmittelmonat wird mit dem Durchschnitt aller Monate verglichen\nein Balken je Monat, die Anfrage nennt Balken\n'wo mein Durchschnitt liegt' ist eine Linie quer durchs Bild, keine zweite Spalte\ndie Spalten sind month und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar and the request asks where the average lies, so barY and one ruleY\nthe columns are month and total_eur, and total_eur holds the numbers\nthe rule's value is the average of these rows, mean(data, 'total_eur'), never a typed figure\nmonths on x with monthShort, euros on y from the bare factory because bars rest on zero"}
{"id": "c18-student-en-011", "household": "student", "language": "en", "request": "Compare what I spent on groceries with what I spent on eating out, month by month in 2025.", "shape": "line", "difficulty": 2, "plan": {"title": "Groceries against eating out", "question": "spending on the categories Groceries and Dining per month of 2025, one row per month and category, columns month as 'YYYY-MM', topic and total_eur", "columns": ["month", "topic", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, category AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND category IN ('Groceries', 'Dining')\nGROUP BY month, topic\nORDER BY month, topic", "code": "const amounts = data.map((row) => row.total_eur);\nreturn defineChart({\n  marks: [\n    lineY(data, { x: 'month', y: 'total_eur', z: 'topic', color: 'topic', strokeWidth: 2, points: true }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.06), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: {\n      scale: scaleLinear().domain([Math.min(0, ...amounts), Math.max(0, ...amounts)]),\n      nice: true,\n      grid: true,\n      axis: { ticks: { format: eurShort } },\n    },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\ntwo categories are followed over twelve months, so two series over one period\na line per category reads the two runs against each other\ntwo names is well under the six colours, so both are asked for by name\nthe columns are month, topic and total_eur, the euro figure last", "reasoning_code": "the shape is line and these rows carry a series, so one lineY over all of them\nthe columns are month, topic and total_eur, and total_eur holds the numbers\ntopic names the series, so it is the z and the color channel and the legend goes on\nmonths on x with monthShort, euros on y over every figure in the rows, zero included"}
{"id": "c18-student-de-012", "household": "student", "language": "de", "request": "Wie viel habe ich in jedem Quartal 2025 ausgegeben?", "shape": "bar", "difficulty": 1, "plan": {"title": "Ausgaben je Quartal", "question": "Ausgaben je Quartal 2025, eine Zeile je Quartal, geordnet, Spalten quarter als '2025-Q1' und total_eur", "columns": ["quarter", "total_eur"]}, "sql": "SELECT '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY quarter\nORDER BY quarter", "code": "return defineChart({\n  marks: [\n    barY(data, { x: 'quarter', y: 'total_eur', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.26), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => point.datum.quarter + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nvier Quartale werden nebeneinander verglichen, eine Zahl je Quartal\nein Balken je Quartal liest sich am direktesten\ndie Anfrage nennt Quartale, also traegt die Achse Quartale und keine Monate\ndie Spalten sind quarter und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar, so the mark is barY\nthe columns are quarter and total_eur, and total_eur holds the numbers\na quarter reads 2025-Q1, which is a name and not a month, so no monthShort and no label dropped\na bar rests on zero, so its euro axis is the bare scaleLinear factory"}
{"id": "c18-student-en-013", "household": "student", "language": "en", "request": "Compare how my spending piled up in the third quarter of 2025 against the fourth.", "shape": "area", "difficulty": 3, "plan": {"title": "Spending added up, third quarter against fourth", "question": "spending of the third and the fourth quarter of 2025 added up inside each quarter, one row per quarter and per month of that quarter, the running total per quarter, columns month_of_quarter as 1 to 3, quarter as '2025-Q3' or '2025-Q4' and cumulative_eur", "columns": ["month_of_quarter", "quarter", "cumulative_eur"]}, "sql": "SELECT CAST(strftime('%m', booked_on) AS INTEGER) - 3 * (((CAST(strftime('%m', booked_on) AS INTEGER) - 1) / 3)) AS month_of_quarter,\n       '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter,\n       ROUND(SUM(-SUM(amount)) OVER (PARTITION BY '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) ORDER BY CAST(strftime('%m', booked_on) AS INTEGER)), 2) AS cumulative_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-07-01' AND '2025-12-31'\nGROUP BY month_of_quarter, quarter\nORDER BY quarter, month_of_quarter", "code": "return defineChart({\n  marks: [\n    areaY(data, { x: 'month_of_quarter', y1: 0, y2: 'cumulative_eur', z: 'quarter', color: 'quarter', fillOpacity: 0.18 }),\n    lineY(data, { x: 'month_of_quarter', y: 'cumulative_eur', z: 'quarter', color: 'quarter', strokeWidth: 2 }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.02), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.quarter + ' ' + point.datum.month_of_quarter + ': ' + eur(point.datum.cumulative_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\n'piled up' is a running total, so an area, and two quarters are two bands\nthe quarter is the series, so one band per quarter and a legend\nthe axis is the month inside the quarter, 1 to 3, so both bands start at the same place\nthe columns are month_of_quarter, quarter and cumulative_eur, the euro figure last", "reasoning_code": "the shape is area and these rows carry a series, so one areaY and one lineY over all of them\nthe columns are month_of_quarter, quarter and cumulative_eur, and cumulative_eur holds the numbers\nquarter names the series, so it is the z and the color channel on both marks and the legend goes on\nthe two bands are compared and not added, so each rests on zero itself: y1 zero, y2 the figure\nan area rests on zero, so its euro axis is the bare scaleLinear factory"}
{"id": "c18-student-de-014", "household": "student", "language": "de", "request": "Wie hat sich mein Oktober 2025 auf die Kategorien aufgeteilt?", "shape": "doughnut", "difficulty": 1, "plan": {"title": "Oktober nach Kategorie", "question": "die sechs groessten Ausgabenkategorien im Oktober 2025 mit ihren Summen, eine Zeile je Kategorie, Spalten topic und total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-10-01' AND '2025-10-31'\nGROUP BY topic\nORDER BY total_eur DESC\nLIMIT 6", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nconst slices = pie(data, { value: 'total_eur' });\nreturn defineChart({\n  marks: [\n    polar({\n      inset: 6,\n      radiusRatio: 0.92,\n      marks: [\n        radialArc(slices, {\n          innerRadius: ({ radius }) => radius * 0.62,\n          color: (slice) => short(slice.topic),\n          key: 'topic',\n        }),\n      ],\n      scales: { angle: null, radius: null },\n    }),\n  ],\n  scales: { x: null, y: null },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nsie fragt, wie sich ein Monat aufteilt, das ist ein Anteil am Ganzen\nein Doughnut zeigt Anteile am direktesten\nder Zeitraum ist der Oktober 2025, die Kuebel sind die eigenen Kategorien\ndie Spalten sind topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is doughnut, so pie allocates the slices and radialArc draws them\nthe columns are topic and total_eur, and total_eur is the value pie reads\nthe slices are the series, so color and key both name topic, the column of names\na doughnut has no axes, so scales.x and scales.y are null and the legend is on"}
{"id": "c18-student-en-015", "household": "student", "language": "en", "request": "Put October and November 2025 side by side by category.", "shape": "bar_grouped", "difficulty": 2, "plan": {"title": "October against November", "question": "spending of October 2025 and of November 2025 per category, one row per category and period, period named 'October' or 'November', the six largest categories by total over both months, columns topic, period and total_eur", "columns": ["topic", "period", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic,\n       CASE WHEN strftime('%Y-%m', booked_on) = '2025-10' THEN 'October' ELSE 'November' END AS period,\n       ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\n  AND booked_on BETWEEN '2025-10-01' AND '2025-11-30'\n  AND category IN ('Groceries', 'Dining', 'Cash', 'Shopping', 'Transport', 'Subscriptions')\nGROUP BY topic, period\nORDER BY topic, period", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'topic', y: 'total_eur', z: 'period', color: (row) => short(row.period), layout: group({ padding: 0.12 }), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.2), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.period + ' ' + point.datum.topic + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\ntwo months are compared and the breakdown is by category, so two dimensions that cross\ngrouped bars put October beside November for each category\nthe period is the series and the category is the position, so the rows come long\nthe columns are topic, period and total_eur, the euro figure last", "reasoning_code": "the shape is bar_grouped, so barY carries a series and layout: group()\nthe columns are topic, period and total_eur, and total_eur holds the numbers\nperiod is the series, so it is the z and the color channel and it needs a legend\ncategory names sit on x, so no label is dropped and they are tilted\neuros on y from the bare factory, because bars rest on zero themselves"}
{"id": "c18-student-de-016", "household": "student", "language": "de", "request": "Zeig gestapelt, wie sich meine Ausgaben im ersten und im zweiten Halbjahr 2025 auf die Kategorien verteilen.", "shape": "bar_stacked", "difficulty": 2, "plan": {"title": "Kategorien je Halbjahr", "question": "Ausgaben der Kategorien Groceries, Cash, Dining, Shopping und Transport je Halbjahr 2025, eine Zeile je Halbjahr und Kategorie, das Halbjahr heisst 'Erstes Halbjahr' oder 'Zweites Halbjahr', Spalten half, topic und total_eur", "columns": ["half", "topic", "total_eur"]}, "sql": "SELECT CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'Erstes Halbjahr' ELSE 'Zweites Halbjahr' END AS half,\n       category AS topic,\n       ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND category IN ('Groceries', 'Cash', 'Dining', 'Shopping', 'Transport')\nGROUP BY half, topic\nORDER BY half, topic", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'half', y: 'total_eur', z: 'topic', color: (row) => short(row.topic), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.22), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + point.datum.half + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nzwei Halbjahre werden je Kategorie verglichen, zwei Dimensionen, die sich kreuzen\ngestapelte Balken zeigen die Verteilung und die Halbjahressumme zugleich\ndas Halbjahr steht auf der Achse, die Kategorie ist die Farbe\ndie Spalten sind half, topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar_stacked, so barY carries a series and no layout\nthe columns are half, topic and total_eur, and total_eur holds the numbers\ntopic is the series, so it is the z and the color channel and it needs a legend\nthe half is a name and not a month, so no monthShort and no label dropped\na stack is taller than any single row, so the euro axis names no domain"}
{"id": "c18-student-en-017", "household": "student", "language": "en", "request": "Rank my categories by what I spent in the second half of 2025.", "shape": "bar_horizontal", "difficulty": 2, "plan": {"title": "Categories in the second half", "question": "spending per category from July to December 2025, ordered by the total, one row per category, columns topic and total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-07-01' AND '2025-12-31'\nGROUP BY topic\nORDER BY total_eur DESC", "code": "return defineChart({\n  marks: [\n    barX(data, { x: 'total_eur', y: 'topic', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n    y: { scale: () => scaleBand().padding(0.22), axis: { tickLabels: { thin: false } } },\n  },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "the request is written in English, so language en\nit ranks the categories of one period against each other\nlong category names read best lying down, so horizontal bars\nthe period is the second half of 2025, July to December\nthe columns are topic and total_eur, the euro figure last", "reasoning_code": "the shape is bar_horizontal, so the mark is barX and nothing else\nthe columns are topic and total_eur, and total_eur holds the numbers\nbarX draws sideways, so the euro column is x and the names are y\nscales.x is the euro axis, scales.y the band of names, none of them dropped"}
{"id": "c18-student-de-018", "household": "student", "language": "de", "request": "Zeig als Sankey, wohin das Geld im zweiten Halbjahr 2025 gegangen ist.", "shape": "sankey", "difficulty": 2, "plan": {"title": "Zweites Halbjahr in die Kategorien", "question": "Ausgaben von Juli bis Dezember 2025 je Kategorie als Fluss, eine Zeile je Kategorie, die Quelle heisst fuer jede Zeile 'Zweites Halbjahr' und die Kategorie ist das Ziel, jeder Betrag positiv, Quelle und Ziel nie derselbe Name und keine Summenzeile, Spalten source, target und amount_eur", "columns": ["source", "target", "amount_eur"]}, "sql": "SELECT 'Zweites Halbjahr' AS source,\n       category AS target,\n       ROUND(-SUM(amount), 2) AS amount_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-07-01' AND '2025-12-31'\n  AND category IN ('Groceries', 'Cash', 'Transport', 'Dining', 'Shopping')\nGROUP BY source, target\nORDER BY amount_eur DESC", "code": "const names = [];\ndata.forEach((row) => {\n  if (names.indexOf(row.source) === -1) names.push(row.source);\n  if (names.indexOf(row.target) === -1) names.push(row.target);\n});\nconst nodes = names.map((name) => ({ id: name }));\nreturn defineChart({\n  marks: [\n    sankeyDiagram({\n      nodes: nodes,\n      links: data,\n      nodeKey: 'id',\n      source: 'source',\n      target: 'target',\n      value: 'amount_eur',\n      align: 'justify',\n      nodeWidth: 14,\n      nodePadding: 8,\n      inset: 8,\n      marks: (context) => [\n        link(context.links, {\n          x1: 'x1',\n          y1: 'y1',\n          x2: 'x2',\n          y2: 'y2',\n          key: 'key',\n          stroke: palette[1],\n          strokeOpacity: 0.3,\n          strokeWidth: (flow) => Math.max(1, flow.width),\n          lineCap: 'butt',\n        }),\n        rect(context.nodes, { x1: 'x0', x2: 'x1', y1: 'y0', y2: 'y1', key: 'key', fill: palette[0], inset: 0 }),\n        text(context.nodes, {\n          x: 'x',\n          y: 'y',\n          key: 'key',\n          text: (node) => node.data.id,\n          dx: (node) => (node.depth === 0 ? 12 : -12),\n          anchor: (node) => (node.depth === 0 ? 'start' : 'end'),\n          fontSize: 11,\n        }),\n      ],\n    }),\n  ],\n  scales: { x: null, y: null },\n  guides: false,\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.source + ' nach ' + point.datum.target + ': ' + eur(point.datum.amount_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nein Halbjahr fliesst in seine Kategorien, das ist ein Fluss von einer Quelle zu Zielen\nein Sankey zeigt, wohin das Geld des Halbjahrs geht\njede Zeile ist ein Fluss mit positivem Betrag, Quelle und Ziel verschieden\ndie Spalten sind source, target und amount_eur", "reasoning_code": "the shape is sankey, so sankeyDiagram lays the flow out\nthe columns are source, target and amount_eur, and amount_eur holds the numbers\nthe nodes are the names of both ends, collected from the rows themselves\nthe child marks read the layout's own fields (x0, x1, y0, y1, key), never a column of the query"}
{"id": "c18-family-en-019", "household": "family", "language": "en", "request": "Compare Kaufland, EDEKA and REWE month by month in 2025 as lines.", "shape": "line", "difficulty": 3, "plan": {"title": "Three supermarkets over the year", "question": "spending at Kaufland, EDEKA and REWE per month of 2025, one row per month and shop, columns month as 'YYYY-MM', merchant and total_eur", "columns": ["month", "merchant", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month,\n       coalesce(title, counterparty, description) AS merchant,\n       ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\n  AND (lower(description || ' ' || coalesce(counterparty, '')) LIKE '%kaufland%'\n       OR lower(description || ' ' || coalesce(counterparty, '')) LIKE '%edeka%'\n       OR lower(description || ' ' || coalesce(counterparty, '')) LIKE '%rewe%')\nGROUP BY month, merchant\nORDER BY month, merchant", "code": "const amounts = data.map((row) => row.total_eur);\nreturn defineChart({\n  marks: [\n    lineY(data, { x: 'month', y: 'total_eur', z: 'merchant', color: 'merchant', strokeWidth: 2, points: true }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.06), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: {\n      scale: scaleLinear().domain([Math.min(0, ...amounts), Math.max(0, ...amounts)]),\n      nice: true,\n      grid: true,\n      axis: { ticks: { format: eurShort } },\n    },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.merchant + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\nthree shops are followed over twelve months, so three series over one period\nit says lines, and a line carries a series, so one line per shop and a legend\nthree names is under the six colours, so every one of them is asked for by name\nthe columns are month, merchant and total_eur, the euro figure last", "reasoning_code": "the shape is line and these rows carry a series, so one lineY over all of them\nthe columns are month, merchant and total_eur, and total_eur holds the numbers\nmerchant names the series, so it is the z and the color channel and the legend goes on\nmonths on x with monthShort, euros on y over every figure in the rows, zero included"}
{"id": "c18-family-de-020", "household": "family", "language": "de", "request": "Welche Monate 2025 waren teurer als ihr Vormonat, und um wie viel? Als Balken.", "shape": "bar", "difficulty": 3, "plan": {"title": "Veraenderung zum Vormonat", "question": "fuer jeden Monat 2025 ausser dem ersten die Differenz zwischen seinen Ausgaben und denen des Vormonats, vorzeichenbehaftet, sodass ein guenstigerer Monat negativ ist, eine Zeile je Monat, Spalten month als 'YYYY-MM' und change_eur", "columns": ["month", "change_eur"]}, "sql": "SELECT month, change_eur\nFROM (\n  SELECT strftime('%Y-%m', booked_on) AS month,\n         ROUND(-SUM(amount) - LAG(-SUM(amount)) OVER (ORDER BY strftime('%Y-%m', booked_on)), 2) AS change_eur\n  FROM transaction_view\n  WHERE amount < 0\n  GROUP BY month\n)\nWHERE change_eur IS NOT NULL\nORDER BY month", "code": "return defineChart({\n  marks: [\n    barY(data, { x: 'month', y: 'change_eur', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.26), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => monthShort(point.datum.month) + ': ' + eur(point.datum.change_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\njeder Monat wird mit dem Monat davor verglichen, eine Zahl je Monat\nBalken stehen auf der Null, also haengt ein guenstigerer Monat darunter\ndie Zahl behaelt ihr Vorzeichen und wird nie zu zwei positiven Spalten\ndie Spalten sind month und change_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar, so the mark is barY\nthe columns are month and change_eur, and change_eur holds the numbers\nmonths on x with monthShort, and the labels may be thinned\na bar rests on zero, so its euro axis is the bare scaleLinear factory and a cheaper month hangs below it"}
{"id": "c18-family-en-021", "household": "family", "language": "en", "request": "Show the running total of our spending for each half of 2025, so I can see which half ran ahead.", "shape": "area", "difficulty": 2, "plan": {"title": "Running total, half against half", "question": "spending of 2025 added up inside each half of the year, one row per half and per month of that half, the running total per half, columns month_of_half as 1 to 6, half as 'First half' or 'Second half' and cumulative_eur", "columns": ["month_of_half", "half", "cumulative_eur"]}, "sql": "SELECT CAST(strftime('%m', booked_on) AS INTEGER) - CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 0 ELSE 6 END AS month_of_half,\n       CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'First half' ELSE 'Second half' END AS half,\n       ROUND(SUM(-SUM(amount)) OVER (PARTITION BY CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'First half' ELSE 'Second half' END ORDER BY CAST(strftime('%m', booked_on) AS INTEGER)), 2) AS cumulative_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month_of_half, half\nORDER BY half, month_of_half", "code": "return defineChart({\n  marks: [\n    areaY(data, { x: 'month_of_half', y1: 0, y2: 'cumulative_eur', z: 'half', color: 'half', fillOpacity: 0.18 }),\n    lineY(data, { x: 'month_of_half', y: 'cumulative_eur', z: 'half', color: 'half', strokeWidth: 2 }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.02), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.half + ' ' + point.datum.month_of_half + ': ' + eur(point.datum.cumulative_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\na running total is an area, and two halves are two bands\nthe half is the series, so one band per half and a legend\nthe axis is the month inside the half, 1 to 6, so both bands start at the same place\nthe columns are month_of_half, half and cumulative_eur, the euro figure last", "reasoning_code": "the shape is area and these rows carry a series, so one areaY and one lineY over all of them\nthe columns are month_of_half, half and cumulative_eur, and cumulative_eur holds the numbers\nhalf names the series, so it is the z and the color channel on both marks and the legend goes on\nthe two bands are compared and not added, so each rests on zero itself: y1 zero, y2 the figure\nan area rests on zero, so its euro axis is the bare scaleLinear factory"}
{"id": "c18-family-de-022", "household": "family", "language": "de", "request": "Wie hat sich der Juli 2025 auf die Kategorien verteilt?", "shape": "doughnut", "difficulty": 1, "plan": {"title": "Juli nach Kategorie", "question": "die sechs groessten Ausgabenkategorien im Juli 2025 mit ihren Summen, eine Zeile je Kategorie, Spalten topic und total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-07-01' AND '2025-07-31'\nGROUP BY topic\nORDER BY total_eur DESC\nLIMIT 6", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nconst slices = pie(data, { value: 'total_eur' });\nreturn defineChart({\n  marks: [\n    polar({\n      inset: 6,\n      radiusRatio: 0.92,\n      marks: [\n        radialArc(slices, {\n          innerRadius: ({ radius }) => radius * 0.62,\n          color: (slice) => short(slice.topic),\n          key: 'topic',\n        }),\n      ],\n      scales: { angle: null, radius: null },\n    }),\n  ],\n  scales: { x: null, y: null },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nsie fragt, wie sich ein Monat aufteilt, das ist ein Anteil am Ganzen\nein Doughnut zeigt Anteile am direktesten\nder Zeitraum ist der Juli 2025, der Urlaubsmonat, die Kuebel sind die eigenen Kategorien\ndie Spalten sind topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is doughnut, so pie allocates the slices and radialArc draws them\nthe columns are topic and total_eur, and total_eur is the value pie reads\nthe slices are the series, so color and key both name topic, the column of names\na doughnut has no axes, so scales.x and scales.y are null and the legend is on"}
{"id": "c18-family-en-023", "household": "family", "language": "en", "request": "Compare the four quarters of 2025 by category as grouped bars.", "shape": "bar_grouped", "difficulty": 3, "plan": {"title": "The four quarters by category", "question": "spending on the categories Groceries, Transport, Insurance, Shopping and Dining per quarter of 2025, one row per category and quarter, columns topic, quarter as '2025-Q1' and total_eur", "columns": ["topic", "quarter", "total_eur"]}, "sql": "SELECT category AS topic, '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\n  AND category IN ('Groceries', 'Transport', 'Insurance', 'Shopping', 'Dining')\nGROUP BY topic, quarter\nORDER BY topic, quarter", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'topic', y: 'total_eur', z: 'quarter', color: (row) => short(row.quarter), layout: group({ padding: 0.12 }), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.2), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.quarter + ' ' + point.datum.topic + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\nfour quarters are compared per category, so two dimensions that cross\ngrouped bars put the four quarters side by side for each category\nthe quarter is the series and the category is the position, so the rows come long\nthe columns are topic, quarter and total_eur, the euro figure last", "reasoning_code": "the shape is bar_grouped, so barY carries a series and layout: group()\nthe columns are topic, quarter and total_eur, and total_eur holds the numbers\nquarter is the series, so it is the z and the color channel and it needs a legend\ncategory names sit on x, so no label is dropped and they are tilted\neuros on y from the bare factory, because bars rest on zero themselves"}
{"id": "c18-family-de-024", "household": "family", "language": "de", "request": "Zeig pro Quartal gestapelt Lebensmittel, Transport und Versicherung.", "shape": "bar_stacked", "difficulty": 2, "plan": {"title": "Drei Kategorien je Quartal", "question": "Ausgaben fuer die Kategorien Groceries, Transport und Insurance je Quartal 2025, eine Zeile je Quartal und Kategorie, Spalten quarter als '2025-Q1', topic und total_eur", "columns": ["quarter", "topic", "total_eur"]}, "sql": "SELECT '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter, category AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND category IN ('Groceries', 'Transport', 'Insurance')\nGROUP BY quarter, topic\nORDER BY quarter, topic", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'quarter', y: 'total_eur', z: 'topic', color: (row) => short(row.topic), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.22), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + point.datum.quarter + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\ndrei Kategorien werden ueber vier Quartale verglichen, zwei Dimensionen, die sich kreuzen\ngestapelte Balken zeigen die Verteilung und die Quartalssumme zugleich\ndie Anfrage nennt Quartale, also steht das Quartal auf der Achse und nie ein Monat\ndie Spalten sind quarter, topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar_stacked, so barY carries a series and no layout\nthe columns are quarter, topic and total_eur, and total_eur holds the numbers\ntopic is the series, so it is the z and the color channel and it needs a legend\na quarter is a name and not a month, so no monthShort and no label dropped\na stack is taller than any single row, so the euro axis names no domain"}
{"id": "c18-family-en-025", "household": "family", "language": "en", "request": "Which categories cost us more in the second half of 2025 than in the first? Rank them by the difference.", "shape": "bar_horizontal", "difficulty": 3, "plan": {"title": "Categories by change between the halves", "question": "for each spending category the difference between what was spent from July to December 2025 and what was spent from January to June 2025, signed so that a cheaper category is negative, ordered by that difference, one row per category, columns topic and change_eur", "columns": ["topic", "change_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic,\n       ROUND(SUM(CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) > 6 THEN -amount ELSE 0 END)\n             - SUM(CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN -amount ELSE 0 END), 2) AS change_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY topic\nORDER BY change_eur DESC", "code": "return defineChart({\n  marks: [\n    barX(data, { x: 'change_eur', y: 'topic', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n    y: { scale: () => scaleBand().padding(0.22), axis: { tickLabels: { thin: false } } },\n  },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.change_eur) },\n});", "reasoning_plan": "the request is written in English, so language en\nit ranks categories by how much they moved between the two halves of the year\nlong category names read best lying down, so horizontal bars\nthe difference keeps its sign, so a cheaper category points left of zero\nthe columns are topic and change_eur, the euro figure last", "reasoning_code": "the shape is bar_horizontal, so the mark is barX and nothing else\nthe columns are topic and change_eur, and change_eur holds the numbers\nbarX draws sideways, so the euro column is x and the names are y\nscales.x is the euro axis from the bare factory, scales.y the band of names, none dropped"}
{"id": "c18-family-de-026", "household": "family", "language": "de", "request": "Zeig als Sankey, wohin unser Geld im Juli 2025 geflossen ist, dem Urlaubsmonat.", "shape": "sankey", "difficulty": 2, "plan": {"title": "Juli in die Kategorien", "question": "Ausgaben des Juli 2025 je Kategorie als Fluss, eine Zeile je Kategorie, die Quelle heisst fuer jede Zeile 'Juli' und die Kategorie ist das Ziel, jeder Betrag positiv, Quelle und Ziel nie derselbe Name und keine Summenzeile, Spalten source, target und amount_eur", "columns": ["source", "target", "amount_eur"]}, "sql": "SELECT 'Juli' AS source,\n       category AS target,\n       ROUND(-SUM(amount), 2) AS amount_eur\nFROM transaction_view\nWHERE amount < 0\n  AND booked_on BETWEEN '2025-07-01' AND '2025-07-31'\n  AND category IN ('Groceries', 'Transport', 'Cash', 'Dining', 'Shopping')\nGROUP BY source, target\nORDER BY amount_eur DESC", "code": "const names = [];\ndata.forEach((row) => {\n  if (names.indexOf(row.source) === -1) names.push(row.source);\n  if (names.indexOf(row.target) === -1) names.push(row.target);\n});\nconst nodes = names.map((name) => ({ id: name }));\nreturn defineChart({\n  marks: [\n    sankeyDiagram({\n      nodes: nodes,\n      links: data,\n      nodeKey: 'id',\n      source: 'source',\n      target: 'target',\n      value: 'amount_eur',\n      align: 'justify',\n      nodeWidth: 14,\n      nodePadding: 8,\n      inset: 8,\n      marks: (context) => [\n        link(context.links, {\n          x1: 'x1',\n          y1: 'y1',\n          x2: 'x2',\n          y2: 'y2',\n          key: 'key',\n          stroke: palette[1],\n          strokeOpacity: 0.3,\n          strokeWidth: (flow) => Math.max(1, flow.width),\n          lineCap: 'butt',\n        }),\n        rect(context.nodes, { x1: 'x0', x2: 'x1', y1: 'y0', y2: 'y1', key: 'key', fill: palette[0], inset: 0 }),\n        text(context.nodes, {\n          x: 'x',\n          y: 'y',\n          key: 'key',\n          text: (node) => node.data.id,\n          dx: (node) => (node.depth === 0 ? 12 : -12),\n          anchor: (node) => (node.depth === 0 ? 'start' : 'end'),\n          fontSize: 11,\n        }),\n      ],\n    }),\n  ],\n  scales: { x: null, y: null },\n  guides: false,\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.source + ' nach ' + point.datum.target + ': ' + eur(point.datum.amount_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nein Monat fliesst in seine Kategorien, das ist ein Fluss von einer Quelle zu Zielen\nein Sankey zeigt, wohin das Geld des Monats geht\njede Zeile ist ein Fluss mit positivem Betrag, Quelle und Ziel verschieden\ndie Spalten sind source, target und amount_eur", "reasoning_code": "the shape is sankey, so sankeyDiagram lays the flow out\nthe columns are source, target and amount_eur, and amount_eur holds the numbers\nthe nodes are the names of both ends, collected from the rows themselves\nthe child marks read the layout's own fields (x0, x1, y0, y1, key), never a column of the query"}
{"id": "c18-freelancer-en-027", "household": "freelancer", "language": "en", "request": "Compare what came in with what went out, month by month in 2025.", "shape": "line", "difficulty": 3, "plan": {"title": "Income against spending per month", "question": "income and spending per month of 2025 from a UNION of the two, one row per month and topic, the topic named 'Income' or 'Spending', both figures positive, columns month as 'YYYY-MM', topic and total_eur", "columns": ["month", "topic", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, 'Income' AS topic, ROUND(SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount > 0\nGROUP BY month\nUNION ALL\nSELECT strftime('%Y-%m', booked_on) AS month, 'Spending' AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month\nORDER BY month, topic", "code": "const amounts = data.map((row) => row.total_eur);\nreturn defineChart({\n  marks: [\n    lineY(data, { x: 'month', y: 'total_eur', z: 'topic', color: 'topic', strokeWidth: 2, points: true }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.06), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: {\n      scale: scaleLinear().domain([Math.min(0, ...amounts), Math.max(0, ...amounts)]),\n      nice: true,\n      grid: true,\n      axis: { ticks: { format: eurShort } },\n    },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\nincome and spending are two runs over the same twelve months\na line per topic reads the two against each other, and the gap is the answer\ntwo figures in one row cannot be two strokes, so the rows come long from a UNION\nthe columns are month, topic and total_eur, the euro figure last", "reasoning_code": "the shape is line and these rows carry a series, so one lineY over all of them\nthe columns are month, topic and total_eur, and total_eur holds the numbers\ntopic names the series, so it is the z and the color channel and the legend goes on\nAugust and September carry no income row, which is a gap in one stroke and not a chart that cannot be drawn\nmonths on x with monthShort, euros on y over every figure in the rows, zero included"}
{"id": "c18-freelancer-de-028", "household": "freelancer", "language": "de", "request": "Zeig mir pro Monat 2025 den Saldo: was ist unterm Strich uebrig geblieben?", "shape": "bar", "difficulty": 3, "plan": {"title": "Saldo je Monat", "question": "je Monat 2025 die Summe aller Buchungen mit Vorzeichen, sodass ein Monat mit mehr Ausgaben als Einnahmen negativ ist, eine Zeile je Monat, Spalten month als 'YYYY-MM' und net_eur", "columns": ["month", "net_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, ROUND(SUM(amount), 2) AS net_eur\nFROM transaction_view\nGROUP BY month\nORDER BY month", "code": "return defineChart({\n  marks: [\n    barY(data, { x: 'month', y: 'net_eur', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.26), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => monthShort(point.datum.month) + ': ' + eur(point.datum.net_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nder Saldo ist eine Zahl je Monat, die negativ sein kann\nBalken stehen auf der Null, also haengt ein Minusmonat darunter, das ist die ganze Antwort\ndie Zahl behaelt ihr Vorzeichen und wird nie zu zwei positiven Spalten\ndie Spalten sind month und net_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar, so the mark is barY\nthe columns are month and net_eur, and net_eur holds the numbers\nmonths on x with monthShort, and the labels may be thinned\na bar rests on zero, so its euro axis is the bare scaleLinear factory and a month in the red hangs below it"}
{"id": "c18-freelancer-en-029", "household": "freelancer", "language": "en", "request": "Compare how my income piled up over 2025 against how my spending did.", "shape": "area", "difficulty": 3, "plan": {"title": "Income and spending added up", "question": "income and spending of 2025 added up month by month from a UNION of the two, one row per month and topic, the topic named 'Income' or 'Spending', the running total per topic, both figures positive, columns month as 'YYYY-MM', topic and cumulative_eur", "columns": ["month", "topic", "cumulative_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, 'Income' AS topic,\n       ROUND(SUM(SUM(amount)) OVER (ORDER BY strftime('%Y-%m', booked_on)), 2) AS cumulative_eur\nFROM transaction_view\nWHERE amount > 0\nGROUP BY month\nUNION ALL\nSELECT strftime('%Y-%m', booked_on) AS month, 'Spending' AS topic,\n       ROUND(SUM(-SUM(amount)) OVER (ORDER BY strftime('%Y-%m', booked_on)), 2) AS cumulative_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month\nORDER BY month, topic", "code": "return defineChart({\n  marks: [\n    areaY(data, { x: 'month', y1: 0, y2: 'cumulative_eur', z: 'topic', color: 'topic', fillOpacity: 0.18 }),\n    lineY(data, { x: 'month', y: 'cumulative_eur', z: 'topic', color: 'topic', strokeWidth: 2 }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.02), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.cumulative_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\n'piled up' is a running total, so an area, and two topics are two bands\nthe topic is the series, so one band per topic and a legend\nthe two bands are compared and never added, so both run over the same twelve months\nthe columns are month, topic and cumulative_eur, the euro figure last", "reasoning_code": "the shape is area and these rows carry a series, so one areaY and one lineY over all of them\nthe columns are month, topic and cumulative_eur, and cumulative_eur holds the numbers\ntopic names the series, so it is the z and the color channel on both marks and the legend goes on\nthe two bands are compared and not added, so each rests on zero itself: y1 zero, y2 the figure\nmonths on x with monthShort, and an area rests on zero, so its euro axis is the bare factory"}
{"id": "c18-freelancer-de-030", "household": "freelancer", "language": "de", "request": "Wie haben sich meine Ausgaben im dritten Quartal 2025 verteilt, dem Quartal ohne Kundenzahlung?", "shape": "doughnut", "difficulty": 2, "plan": {"title": "Drittes Quartal nach Kategorie", "question": "die sechs groessten Ausgabenkategorien im dritten Quartal 2025, also Juli bis September, mit ihren Summen, eine Zeile je Kategorie, Spalten topic und total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-07-01' AND '2025-09-30'\nGROUP BY topic\nORDER BY total_eur DESC\nLIMIT 6", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nconst slices = pie(data, { value: 'total_eur' });\nreturn defineChart({\n  marks: [\n    polar({\n      inset: 6,\n      radiusRatio: 0.92,\n      marks: [\n        radialArc(slices, {\n          innerRadius: ({ radius }) => radius * 0.62,\n          color: (slice) => short(slice.topic),\n          key: 'topic',\n        }),\n      ],\n      scales: { angle: null, radius: null },\n    }),\n  ],\n  scales: { x: null, y: null },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nsie fragt, wie sich ein Quartal aufteilt, das ist ein Anteil am Ganzen\nein Doughnut zeigt Anteile am direktesten\nder Zeitraum ist das dritte Quartal 2025, Juli bis September\ndie Spalten sind topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is doughnut, so pie allocates the slices and radialArc draws them\nthe columns are topic and total_eur, and total_eur is the value pie reads\nthe slices are the series, so color and key both name topic, the column of names\na doughnut has no axes, so scales.x and scales.y are null and the legend is on"}
{"id": "c18-freelancer-en-031", "household": "freelancer", "language": "en", "request": "Compare my four quarters of 2025 category by category, side by side.", "shape": "bar_grouped", "difficulty": 3, "plan": {"title": "The four quarters by category", "question": "spending on the categories Insurance, Groceries, Transport, Subscriptions and Dining per quarter of 2025, one row per category and quarter, columns topic, quarter as '2025-Q1' and total_eur", "columns": ["topic", "quarter", "total_eur"]}, "sql": "SELECT category AS topic, '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\n  AND category IN ('Insurance', 'Groceries', 'Transport', 'Subscriptions', 'Dining')\nGROUP BY topic, quarter\nORDER BY topic, quarter", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'topic', y: 'total_eur', z: 'quarter', color: (row) => short(row.quarter), layout: group({ padding: 0.12 }), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.2), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.quarter + ' ' + point.datum.topic + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\nfour quarters are compared per category, so two dimensions that cross\ngrouped bars put the four quarters side by side for each category\nthe quarter is the series and the category is the position, so the rows come long\nthe columns are topic, quarter and total_eur, the euro figure last", "reasoning_code": "the shape is bar_grouped, so barY carries a series and layout: group()\nthe columns are topic, quarter and total_eur, and total_eur holds the numbers\nquarter is the series, so it is the z and the color channel and it needs a legend\ncategory names sit on x, so no label is dropped and they are tilted\neuros on y from the bare factory, because bars rest on zero themselves"}
{"id": "c18-freelancer-de-032", "household": "freelancer", "language": "de", "request": "Zeig gestapelt pro Quartal, wie sich Versicherung, Lebensmittel, Transport und Abos verteilen.", "shape": "bar_stacked", "difficulty": 2, "plan": {"title": "Vier Kategorien je Quartal", "question": "Ausgaben fuer die Kategorien Insurance, Groceries, Transport und Subscriptions je Quartal 2025, eine Zeile je Quartal und Kategorie, Spalten quarter als '2025-Q1', topic und total_eur", "columns": ["quarter", "topic", "total_eur"]}, "sql": "SELECT '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter, category AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\n  AND category IN ('Insurance', 'Groceries', 'Transport', 'Subscriptions')\nGROUP BY quarter, topic\nORDER BY quarter, topic", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'quarter', y: 'total_eur', z: 'topic', color: (row) => short(row.topic), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.22), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + point.datum.quarter + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nvier Kategorien werden ueber vier Quartale verglichen, zwei Dimensionen, die sich kreuzen\ngestapelte Balken zeigen die Verteilung und die Quartalssumme zugleich\ndie Anfrage nennt Quartale, also steht das Quartal auf der Achse und nie ein Monat\ndie Spalten sind quarter, topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar_stacked, so barY carries a series and no layout\nthe columns are quarter, topic and total_eur, and total_eur holds the numbers\ntopic is the series, so it is the z and the color channel and it needs a legend\na quarter is a name and not a month, so no monthShort and no label dropped\na stack is taller than any single row, so the euro axis names no domain"}
{"id": "c18-freelancer-en-033", "household": "freelancer", "language": "en", "request": "Order the months of 2025 by their swing against the month before, biggest bar first.", "shape": "bar_horizontal", "difficulty": 3, "plan": {"title": "Months by change to the month before", "question": "for each month of 2025 after the first the difference between its spending and the spending of the month before, signed so that a cheaper month is negative, ordered by that difference, one row per month, columns month as 'YYYY-MM' and change_eur", "columns": ["month", "change_eur"]}, "sql": "SELECT month, change_eur\nFROM (\n  SELECT strftime('%Y-%m', booked_on) AS month,\n         ROUND(-SUM(amount) - LAG(-SUM(amount)) OVER (ORDER BY strftime('%Y-%m', booked_on)), 2) AS change_eur\n  FROM transaction_view\n  WHERE amount < 0\n  GROUP BY month\n)\nWHERE change_eur IS NOT NULL\nORDER BY change_eur DESC", "code": "return defineChart({\n  marks: [\n    barX(data, { x: 'change_eur', y: 'month', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n    y: { scale: () => scaleBand().padding(0.22), axis: { ticks: { format: monthShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => monthShort(point.datum.month) + ': ' + eur(point.datum.change_eur) },\n});", "reasoning_plan": "the request is written in English, so language en\nit ranks the months by how far each moved from the one before it\neleven bars ordered by their figure read best lying down, so horizontal bars\nthe difference keeps its sign, so a cheaper month points left of zero\nthe columns are month and change_eur, the euro figure last", "reasoning_code": "the shape is bar_horizontal, so the mark is barX and nothing else\nthe columns are month and change_eur, and change_eur holds the numbers\nbarX draws sideways, so the euro column is x and the months are y with monthShort\nscales.x is the euro axis from the bare factory, so the negative bars run left of zero"}
{"id": "c18-freelancer-de-034", "household": "freelancer", "language": "de", "request": "Zeig als Sankey, wohin das Geld im dritten Quartal 2025 geflossen ist.", "shape": "sankey", "difficulty": 2, "plan": {"title": "Drittes Quartal in die Kategorien", "question": "Ausgaben des dritten Quartals 2025 je Kategorie als Fluss, eine Zeile je Kategorie, die Quelle heisst fuer jede Zeile 'Drittes Quartal' und die Kategorie ist das Ziel, jeder Betrag positiv, Quelle und Ziel nie derselbe Name und keine Summenzeile, Spalten source, target und amount_eur", "columns": ["source", "target", "amount_eur"]}, "sql": "SELECT 'Drittes Quartal' AS source, category AS target, ROUND(-SUM(amount), 2) AS amount_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-07-01' AND '2025-09-30'\n  AND category IN ('Insurance', 'Groceries', 'Transport', 'Subscriptions', 'Dining')\nGROUP BY source, target\nORDER BY amount_eur DESC", "code": "const names = [];\ndata.forEach((row) => {\n  if (names.indexOf(row.source) === -1) names.push(row.source);\n  if (names.indexOf(row.target) === -1) names.push(row.target);\n});\nconst nodes = names.map((name) => ({ id: name }));\nreturn defineChart({\n  marks: [\n    sankeyDiagram({\n      nodes: nodes,\n      links: data,\n      nodeKey: 'id',\n      source: 'source',\n      target: 'target',\n      value: 'amount_eur',\n      align: 'justify',\n      nodeWidth: 14,\n      nodePadding: 8,\n      inset: 8,\n      marks: (context) => [\n        link(context.links, {\n          x1: 'x1',\n          y1: 'y1',\n          x2: 'x2',\n          y2: 'y2',\n          key: 'key',\n          stroke: palette[1],\n          strokeOpacity: 0.3,\n          strokeWidth: (flow) => Math.max(1, flow.width),\n          lineCap: 'butt',\n        }),\n        rect(context.nodes, { x1: 'x0', x2: 'x1', y1: 'y0', y2: 'y1', key: 'key', fill: palette[0], inset: 0 }),\n        text(context.nodes, {\n          x: 'x',\n          y: 'y',\n          key: 'key',\n          text: (node) => node.data.id,\n          dx: (node) => (node.depth === 0 ? 12 : -12),\n          anchor: (node) => (node.depth === 0 ? 'start' : 'end'),\n          fontSize: 11,\n        }),\n      ],\n    }),\n  ],\n  scales: { x: null, y: null },\n  guides: false,\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.source + ' nach ' + point.datum.target + ': ' + eur(point.datum.amount_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nein Quartal fliesst in seine Kategorien, das ist ein Fluss von einer Quelle zu Zielen\nein Sankey zeigt, wohin das Geld des Quartals geht\njede Zeile ist ein Fluss mit positivem Betrag, Quelle und Ziel verschieden\ndie Spalten sind source, target und amount_eur", "reasoning_code": "the shape is sankey, so sankeyDiagram lays the flow out\nthe columns are source, target and amount_eur, and amount_eur holds the numbers\nthe nodes are the names of both ends, collected from the rows themselves\nthe child marks read the layout's own fields (x0, x1, y0, y1, key), never a column of the query"}
{"id": "c18-pensioner-en-035", "household": "pensioner", "language": "en", "request": "Compare what I spend on groceries with what I spend on health, month by month.", "shape": "line", "difficulty": 2, "plan": {"title": "Groceries against health", "question": "spending on the categories Groceries and Health per month of 2025, one row per month and category, columns month as 'YYYY-MM', topic and total_eur", "columns": ["month", "topic", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, category AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND category IN ('Groceries', 'Health')\nGROUP BY month, topic\nORDER BY month, topic", "code": "const amounts = data.map((row) => row.total_eur);\nreturn defineChart({\n  marks: [\n    lineY(data, { x: 'month', y: 'total_eur', z: 'topic', color: 'topic', strokeWidth: 2, points: true }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.06), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: {\n      scale: scaleLinear().domain([Math.min(0, ...amounts), Math.max(0, ...amounts)]),\n      nice: true,\n      grid: true,\n      axis: { ticks: { format: eurShort } },\n    },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\ntwo categories are followed over twelve months, so two series over one period\na line per category reads the two runs against each other\ntwo names is well under the six colours, so both are asked for by name\nthe columns are month, topic and total_eur, the euro figure last", "reasoning_code": "the shape is line and these rows carry a series, so one lineY over all of them\nthe columns are month, topic and total_eur, and total_eur holds the numbers\ntopic names the series, so it is the z and the color channel and the legend goes on\nmonths on x with monthShort, euros on y over every figure in the rows, zero included"}
{"id": "c18-pensioner-de-036", "household": "pensioner", "language": "de", "request": "In welchen Monaten 2025 lag ich ueber meinem eigenen Schnitt? Balken mit einer Linie fuer den Schnitt.", "shape": "bar", "difficulty": 2, "plan": {"title": "Monatliche Ausgaben mit Durchschnitt", "question": "Ausgaben pro Monat 2025, eine Zeile je Monat, geordnet, Spalten month als 'YYYY-MM' und total_eur", "columns": ["month", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month\nORDER BY month", "code": "return defineChart({\n  marks: [\n    barY(data, { x: 'month', y: 'total_eur', fill: palette[0], maxThickness: 32 }),\n    ruleY([mean(data, 'total_eur')]),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.26), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\njeder Monat wird mit dem Durchschnitt aller Monate verglichen\neine Zahl je Monat, also ein Balken je Monat\n'wo mein Durchschnitt liegt' ist eine Linie quer durchs Bild, keine zweite Spalte\ndie Spalten sind month und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar and the request asks where the average lies, so barY and one ruleY\nthe columns are month and total_eur, and total_eur holds the numbers\nthe rule's value is the average of these rows, mean(data, 'total_eur'), never a typed figure\nmonths on x with monthShort, euros on y from the bare factory because bars rest on zero"}
{"id": "c18-pensioner-en-037", "household": "pensioner", "language": "en", "request": "Compare how my spending added up inside each quarter of 2025.", "shape": "area", "difficulty": 2, "plan": {"title": "Spending added up per quarter", "question": "spending of 2025 added up inside each quarter, one row per quarter and per month of that quarter, the running total per quarter, columns month_of_quarter as 1 to 3, quarter as '2025-Q1' and cumulative_eur", "columns": ["month_of_quarter", "quarter", "cumulative_eur"]}, "sql": "SELECT CAST(strftime('%m', booked_on) AS INTEGER) - 3 * (((CAST(strftime('%m', booked_on) AS INTEGER) - 1) / 3)) AS month_of_quarter,\n       '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter,\n       ROUND(SUM(-SUM(amount)) OVER (PARTITION BY '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) ORDER BY CAST(strftime('%m', booked_on) AS INTEGER)), 2) AS cumulative_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month_of_quarter, quarter\nORDER BY quarter, month_of_quarter", "code": "return defineChart({\n  marks: [\n    areaY(data, { x: 'month_of_quarter', y1: 0, y2: 'cumulative_eur', z: 'quarter', color: 'quarter', fillOpacity: 0.18 }),\n    lineY(data, { x: 'month_of_quarter', y: 'cumulative_eur', z: 'quarter', color: 'quarter', strokeWidth: 2 }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.02), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.quarter + ' ' + point.datum.month_of_quarter + ': ' + eur(point.datum.cumulative_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\n'added up' is a running total, so an area, and four quarters are four bands\nthe quarter is the series, so one band per quarter and a legend\nthe axis is the month inside the quarter, 1 to 3, so every band starts at the same place\nthe columns are month_of_quarter, quarter and cumulative_eur, the euro figure last", "reasoning_code": "the shape is area and these rows carry a series, so one areaY and one lineY over all of them\nthe columns are month_of_quarter, quarter and cumulative_eur, and cumulative_eur holds the numbers\nquarter names the series, so it is the z and the color channel on both marks and the legend goes on\nthe bands are compared and not added, so each rests on zero itself: y1 zero, y2 the figure\nan area rests on zero, so its euro axis is the bare scaleLinear factory"}
{"id": "c18-pensioner-de-038", "household": "pensioner", "language": "de", "request": "Wie hat sich der Februar 2025 auf die Kategorien verteilt?", "shape": "doughnut", "difficulty": 1, "plan": {"title": "Februar nach Kategorie", "question": "die sechs groessten Ausgabenkategorien im Februar 2025 mit ihren Summen, eine Zeile je Kategorie, Spalten topic und total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-02-01' AND '2025-02-28'\nGROUP BY topic\nORDER BY total_eur DESC\nLIMIT 6", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nconst slices = pie(data, { value: 'total_eur' });\nreturn defineChart({\n  marks: [\n    polar({\n      inset: 6,\n      radiusRatio: 0.92,\n      marks: [\n        radialArc(slices, {\n          innerRadius: ({ radius }) => radius * 0.62,\n          color: (slice) => short(slice.topic),\n          key: 'topic',\n        }),\n      ],\n      scales: { angle: null, radius: null },\n    }),\n  ],\n  scales: { x: null, y: null },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nsie fragt, wie sich ein Monat aufteilt, das ist ein Anteil am Ganzen\nein Doughnut zeigt Anteile am direktesten\nder Zeitraum ist der Februar 2025, der Monat der Heizkostenabrechnung\ndie Spalten sind topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is doughnut, so pie allocates the slices and radialArc draws them\nthe columns are topic and total_eur, and total_eur is the value pie reads\nthe slices are the series, so color and key both name topic, the column of names\na doughnut has no axes, so scales.x and scales.y are null and the legend is on"}
{"id": "c18-pensioner-en-039", "household": "pensioner", "language": "en", "request": "Put January to June beside July to December for each of my categories.", "shape": "bar_grouped", "difficulty": 2, "plan": {"title": "First half against second half by category", "question": "spending of the first half and of the second half of 2025 per category, one row per category and period, period named 'First half' or 'Second half', the six largest categories by total over both halves, columns topic, period and total_eur", "columns": ["topic", "period", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic,\n       CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'First half' ELSE 'Second half' END AS period,\n       ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\n  AND category IN ('Cash', 'Groceries', 'Health', 'Insurance', 'Housing', 'Transport')\nGROUP BY topic, period\nORDER BY topic, period", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'topic', y: 'total_eur', z: 'period', color: (row) => short(row.period), layout: group({ padding: 0.12 }), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.2), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.period + ' ' + point.datum.topic + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\ntwo periods are compared and the breakdown is by category, so two dimensions that cross\ngrouped bars put the two halves side by side for each category\nthe period is the series and the category is the position, so the rows come long\nthe columns are topic, period and total_eur, the euro figure last", "reasoning_code": "the shape is bar_grouped, so barY carries a series and layout: group()\nthe columns are topic, period and total_eur, and total_eur holds the numbers\nperiod is the series, so it is the z and the color channel and it needs a legend\ncategory names sit on x, so no label is dropped and they are tilted\neuros on y from the bare factory, because bars rest on zero themselves"}
{"id": "c18-pensioner-de-040", "household": "pensioner", "language": "de", "request": "Zeig pro Monat 2025 gestapelt Lebensmittel, Gesundheit und Bargeld.", "shape": "bar_stacked", "difficulty": 2, "plan": {"title": "Drei Kategorien je Monat", "question": "Ausgaben fuer die Kategorien Groceries, Health und Cash je Monat 2025, eine Zeile je Monat und Kategorie, Spalten month als 'YYYY-MM', topic und total_eur", "columns": ["month", "topic", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, category AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND category IN ('Groceries', 'Health', 'Cash')\nGROUP BY month, topic\nORDER BY month, topic", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nreturn defineChart({\n  marks: [\n    barY(data, { x: 'month', y: 'total_eur', z: 'topic', color: (row) => short(row.topic), maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.22), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\ndrei Kategorien werden ueber zwoelf Monate verglichen, zwei Dimensionen, die sich kreuzen\ngestapelte Balken zeigen die Verteilung und die Monatssumme zugleich\nder Monat steht auf der Achse, die Kategorie ist die Farbe\ndie Spalten sind month, topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar_stacked, so barY carries a series and no layout\nthe columns are month, topic and total_eur, and total_eur holds the numbers\ntopic is the series, so it is the z and the color channel and it needs a legend\nmonths on x with monthShort, and the labels may be thinned\na stack is taller than any single row, so the euro axis names no domain"}
{"id": "c18-pensioner-en-041", "household": "pensioner", "language": "en", "request": "Rank my categories by what I spent in the first quarter of 2025.", "shape": "bar_horizontal", "difficulty": 1, "plan": {"title": "Categories in the first quarter", "question": "spending per category from January to March 2025, ordered by the total, one row per category, columns topic and total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-01-01' AND '2025-03-31'\nGROUP BY topic\nORDER BY total_eur DESC", "code": "return defineChart({\n  marks: [\n    barX(data, { x: 'total_eur', y: 'topic', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n    y: { scale: () => scaleBand().padding(0.22), axis: { tickLabels: { thin: false } } },\n  },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "the request is written in English, so language en\nit ranks the categories of one quarter against each other\nlong category names read best lying down, so horizontal bars\nthe period is the first quarter of 2025, January to March\nthe columns are topic and total_eur, the euro figure last", "reasoning_code": "the shape is bar_horizontal, so the mark is barX and nothing else\nthe columns are topic and total_eur, and total_eur holds the numbers\nbarX draws sideways, so the euro column is x and the names are y\nscales.x is the euro axis, scales.y the band of names, none of them dropped"}
{"id": "c18-pensioner-de-042", "household": "pensioner", "language": "de", "request": "Zeig als Sankey, wohin mein Geld im Februar 2025 geflossen ist, dem Monat der Heizkostenabrechnung.", "shape": "sankey", "difficulty": 2, "plan": {"title": "Februar in die Kategorien", "question": "Ausgaben des Februar 2025 je Kategorie als Fluss, eine Zeile je Kategorie, die Quelle heisst fuer jede Zeile 'Februar' und die Kategorie ist das Ziel, jeder Betrag positiv, Quelle und Ziel nie derselbe Name und keine Summenzeile, Spalten source, target und amount_eur", "columns": ["source", "target", "amount_eur"]}, "sql": "SELECT 'Februar' AS source,\n       category AS target,\n       ROUND(-SUM(amount), 2) AS amount_eur\nFROM transaction_view\nWHERE amount < 0\n  AND booked_on BETWEEN '2025-02-01' AND '2025-02-28'\n  AND category IN ('Cash', 'Groceries', 'Health', 'Insurance', 'Housing')\nGROUP BY source, target\nORDER BY amount_eur DESC", "code": "const names = [];\ndata.forEach((row) => {\n  if (names.indexOf(row.source) === -1) names.push(row.source);\n  if (names.indexOf(row.target) === -1) names.push(row.target);\n});\nconst nodes = names.map((name) => ({ id: name }));\nreturn defineChart({\n  marks: [\n    sankeyDiagram({\n      nodes: nodes,\n      links: data,\n      nodeKey: 'id',\n      source: 'source',\n      target: 'target',\n      value: 'amount_eur',\n      align: 'justify',\n      nodeWidth: 14,\n      nodePadding: 8,\n      inset: 8,\n      marks: (context) => [\n        link(context.links, {\n          x1: 'x1',\n          y1: 'y1',\n          x2: 'x2',\n          y2: 'y2',\n          key: 'key',\n          stroke: palette[1],\n          strokeOpacity: 0.3,\n          strokeWidth: (flow) => Math.max(1, flow.width),\n          lineCap: 'butt',\n        }),\n        rect(context.nodes, { x1: 'x0', x2: 'x1', y1: 'y0', y2: 'y1', key: 'key', fill: palette[0], inset: 0 }),\n        text(context.nodes, {\n          x: 'x',\n          y: 'y',\n          key: 'key',\n          text: (node) => node.data.id,\n          dx: (node) => (node.depth === 0 ? 12 : -12),\n          anchor: (node) => (node.depth === 0 ? 'start' : 'end'),\n          fontSize: 11,\n        }),\n      ],\n    }),\n  ],\n  scales: { x: null, y: null },\n  guides: false,\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.source + ' nach ' + point.datum.target + ': ' + eur(point.datum.amount_eur),\n  },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nein Monat fliesst in seine Kategorien, das ist ein Fluss von einer Quelle zu Zielen\nein Sankey zeigt, wohin das Geld des Monats geht\njede Zeile ist ein Fluss mit positivem Betrag, Quelle und Ziel verschieden\ndie Spalten sind source, target und amount_eur", "reasoning_code": "the shape is sankey, so sankeyDiagram lays the flow out\nthe columns are source, target and amount_eur, and amount_eur holds the numbers\nthe nodes are the names of both ends, collected from the rows themselves\nthe child marks read the layout's own fields (x0, x1, y0, y1, key), never a column of the query"}
{"id": "c18-couple-en-043", "household": "couple", "language": "en", "request": "Compare the joint account and Jonas's personal account month by month in 2025.", "shape": "line", "difficulty": 3, "plan": {"title": "Joint account against personal account", "question": "spending per month of 2025 and per account, one row per month and account, both accounts under their own name, columns month as 'YYYY-MM', account_name and total_eur", "columns": ["month", "account_name", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, account AS account_name, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month, account_name\nORDER BY month, account_name", "code": "const amounts = data.map((row) => row.total_eur);\nreturn defineChart({\n  marks: [\n    lineY(data, { x: 'month', y: 'total_eur', z: 'account_name', color: 'account_name', strokeWidth: 2, points: true }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.06), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: {\n      scale: scaleLinear().domain([Math.min(0, ...amounts), Math.max(0, ...amounts)]),\n      nice: true,\n      grid: true,\n      axis: { ticks: { format: eurShort } },\n    },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.account_name + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\ntwo accounts are followed over twelve months, so two series over one period\na line per account reads the two runs against each other\nthis profile carries both accounts, so each is asked for under its own name\nthe columns are month, account_name and total_eur, the euro figure last", "reasoning_code": "the shape is line and these rows carry a series, so one lineY over all of them\nthe columns are month, account_name and total_eur, and total_eur holds the numbers\naccount_name names the series, so it is the z and the color channel and the legend goes on\nmonths on x with monthShort, euros on y over every figure in the rows, zero included"}
{"id": "c18-couple-de-044", "household": "couple", "language": "de", "request": "Vergleiche die vier Quartale 2025 auf dem Gemeinschaftskonto.", "shape": "bar", "difficulty": 2, "plan": {"title": "Quartale auf dem Gemeinschaftskonto", "question": "Ausgaben je Quartal 2025 nur auf dem Gemeinschaftskonto, eine Zeile je Quartal, geordnet, Spalten quarter als '2025-Q1' und total_eur", "columns": ["quarter", "total_eur"]}, "sql": "SELECT '2025-Q' || ((CAST(strftime('%m', booked_on) AS INTEGER) + 2) / 3) AS quarter, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND account = 'Sparkasse Stuttgart Gemeinschaftskonto'\nGROUP BY quarter\nORDER BY quarter", "code": "return defineChart({\n  marks: [\n    barY(data, { x: 'quarter', y: 'total_eur', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.26), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => point.datum.quarter + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nvier Quartale werden nebeneinander verglichen, eine Zahl je Quartal\nein Balken je Quartal liest sich am direktesten\ndas Profil hat zwei Konten, die Anfrage meint nur das Gemeinschaftskonto\ndie Spalten sind quarter und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar, so the mark is barY\nthe columns are quarter and total_eur, and total_eur holds the numbers\na quarter reads 2025-Q1, which is a name and not a month, so no monthShort and no label dropped\na bar rests on zero, so its euro axis is the bare scaleLinear factory"}
{"id": "c18-couple-en-045", "household": "couple", "language": "en", "request": "Compare how the two accounts' spending added up over 2025.", "shape": "area", "difficulty": 3, "plan": {"title": "Both accounts added up", "question": "spending of 2025 added up month by month inside each account, one row per month and account, the running total per account, columns month as 'YYYY-MM', account_name and cumulative_eur", "columns": ["month", "account_name", "cumulative_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month,\n       account AS account_name,\n       ROUND(SUM(-SUM(amount)) OVER (PARTITION BY account ORDER BY strftime('%Y-%m', booked_on)), 2) AS cumulative_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month, account_name\nORDER BY account_name, month", "code": "return defineChart({\n  marks: [\n    areaY(data, { x: 'month', y1: 0, y2: 'cumulative_eur', z: 'account_name', color: 'account_name', fillOpacity: 0.18 }),\n    lineY(data, { x: 'month', y: 'cumulative_eur', z: 'account_name', color: 'account_name', strokeWidth: 2 }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.02), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.account_name + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.cumulative_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\n'added up' is a running total, so an area, and two accounts are two bands\nthe account is the series, so one band per account and a legend\nboth run over the same twelve months, so the bands are read off the gap between them\nthe columns are month, account_name and cumulative_eur, the euro figure last", "reasoning_code": "the shape is area and these rows carry a series, so one areaY and one lineY over all of them\nthe columns are month, account_name and cumulative_eur, and cumulative_eur holds the numbers\naccount_name names the series, so it is the z and the color channel on both marks and the legend goes on\nthe two bands are compared and not added, so each rests on zero itself: y1 zero, y2 the figure\nmonths on x with monthShort, and an area rests on zero, so its euro axis is the bare factory"}
{"id": "c18-couple-de-046", "household": "couple", "language": "de", "request": "Wie haben sich unsere Ausgaben im zweiten Halbjahr 2025 auf die Kategorien verteilt?", "shape": "doughnut", "difficulty": 1, "plan": {"title": "Zweites Halbjahr nach Kategorie", "question": "die sechs groessten Ausgabenkategorien von Juli bis Dezember 2025 ueber beide Konten mit ihren Summen, eine Zeile je Kategorie, Spalten topic und total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND booked_on BETWEEN '2025-07-01' AND '2025-12-31'\nGROUP BY topic\nORDER BY total_eur DESC\nLIMIT 6", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nconst slices = pie(data, { value: 'total_eur' });\nreturn defineChart({\n  marks: [\n    polar({\n      inset: 6,\n      radiusRatio: 0.92,\n      marks: [\n        radialArc(slices, {\n          innerRadius: ({ radius }) => radius * 0.62,\n          color: (slice) => short(slice.topic),\n          key: 'topic',\n        }),\n      ],\n      scales: { angle: null, radius: null },\n    }),\n  ],\n  scales: { x: null, y: null },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nsie fragt, wie sich ein Halbjahr aufteilt, das ist ein Anteil am Ganzen\nein Doughnut zeigt Anteile am direktesten\n'unsere' meint beide Konten, also wird keines herausgefiltert\ndie Spalten sind topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is doughnut, so pie allocates the slices and radialArc draws them\nthe columns are topic and total_eur, and total_eur is the value pie reads\nthe slices are the series, so color and key both name topic, the column of names\na doughnut has no axes, so scales.x and scales.y are null and the legend is on"}
{"id": "c18-couple-en-047", "household": "couple", "language": "en", "request": "Compare groceries and eating out per month in 2025, across both accounts.", "shape": "line", "difficulty": 2, "plan": {"title": "Groceries against eating out", "question": "spending on the categories Groceries and Dining per month of 2025 over both accounts, one row per month and category, columns month as 'YYYY-MM', topic and total_eur", "columns": ["month", "topic", "total_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month, category AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND category IN ('Groceries', 'Dining')\nGROUP BY month, topic\nORDER BY month, topic", "code": "const amounts = data.map((row) => row.total_eur);\nreturn defineChart({\n  marks: [\n    lineY(data, { x: 'month', y: 'total_eur', z: 'topic', color: 'topic', strokeWidth: 2, points: true }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.06), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: {\n      scale: scaleLinear().domain([Math.min(0, ...amounts), Math.max(0, ...amounts)]),\n      nice: true,\n      grid: true,\n      axis: { ticks: { format: eurShort } },\n    },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.topic + ' ' + monthShort(point.datum.month) + ': ' + eur(point.datum.total_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\ntwo categories are followed over twelve months, so two series over one period\na line per category reads the two runs against each other\n'across both accounts' means neither account is filtered out\nthe columns are month, topic and total_eur, the euro figure last", "reasoning_code": "the shape is line and these rows carry a series, so one lineY over all of them\nthe columns are month, topic and total_eur, and total_eur holds the numbers\ntopic names the series, so it is the z and the color channel and the legend goes on\nmonths on x with monthShort, euros on y over every figure in the rows, zero included"}
{"id": "c18-couple-de-048", "household": "couple", "language": "de", "request": "Zeig mir pro Monat 2025, wie viel mehr auf dem Gemeinschaftskonto als auf dem Privatkonto ausgegeben wurde.", "shape": "bar", "difficulty": 3, "plan": {"title": "Gemeinschaftskonto minus Privatkonto", "question": "je Monat 2025 die Differenz zwischen den Ausgaben auf dem Gemeinschaftskonto und denen auf dem Privatkonto von Jonas, vorzeichenbehaftet, sodass ein Monat mit mehr Ausgaben auf dem Privatkonto negativ ist, eine Zeile je Monat, Spalten month als 'YYYY-MM' und difference_eur", "columns": ["month", "difference_eur"]}, "sql": "SELECT strftime('%Y-%m', booked_on) AS month,\n       ROUND(SUM(CASE WHEN account = 'Sparkasse Stuttgart Gemeinschaftskonto' THEN -amount ELSE 0 END)\n             - SUM(CASE WHEN account = 'Sparkasse Stuttgart Privatkonto Jonas' THEN -amount ELSE 0 END), 2) AS difference_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month\nORDER BY month", "code": "return defineChart({\n  marks: [\n    barY(data, { x: 'month', y: 'difference_eur', fill: palette[0], maxThickness: 32 }),\n  ],\n  scales: {\n    x: { scale: () => scaleBand().padding(0.26), axis: { ticks: { format: monthShort }, tickLabels: { thin: { minGap: 6, priority: 'ends' } } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  tooltip: { use: tooltip, format: (point) => monthShort(point.datum.month) + ': ' + eur(point.datum.difference_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nzwei Konten werden Monat fuer Monat gegeneinander gerechnet, eine Zahl je Monat\nBalken stehen auf der Null, also haengt ein Monat mit mehr Privatausgaben darunter\ndie Differenz behaelt ihr Vorzeichen und wird nie zu zwei positiven Spalten\ndie Spalten sind month und difference_eur, der Eurowert zuletzt", "reasoning_code": "the shape is bar, so the mark is barY\nthe columns are month and difference_eur, and difference_eur holds the numbers\nmonths on x with monthShort, and the labels may be thinned\na bar rests on zero, so its euro axis is the bare scaleLinear factory and a negative month hangs below it"}
{"id": "c18-couple-en-049", "household": "couple", "language": "en", "request": "Compare how our spending added up through the first half of 2025 against the second half.", "shape": "area", "difficulty": 2, "plan": {"title": "Added up, first half against second", "question": "spending of 2025 over both accounts added up inside each half of the year, one row per half and per month of that half, the running total per half, columns month_of_half as 1 to 6, half as 'First half' or 'Second half' and cumulative_eur", "columns": ["month_of_half", "half", "cumulative_eur"]}, "sql": "SELECT CAST(strftime('%m', booked_on) AS INTEGER) - CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 0 ELSE 6 END AS month_of_half,\n       CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'First half' ELSE 'Second half' END AS half,\n       ROUND(SUM(-SUM(amount)) OVER (PARTITION BY CASE WHEN CAST(strftime('%m', booked_on) AS INTEGER) <= 6 THEN 'First half' ELSE 'Second half' END ORDER BY CAST(strftime('%m', booked_on) AS INTEGER)), 2) AS cumulative_eur\nFROM transaction_view\nWHERE amount < 0\nGROUP BY month_of_half, half\nORDER BY half, month_of_half", "code": "return defineChart({\n  marks: [\n    areaY(data, { x: 'month_of_half', y1: 0, y2: 'cumulative_eur', z: 'half', color: 'half', fillOpacity: 0.18 }),\n    lineY(data, { x: 'month_of_half', y: 'cumulative_eur', z: 'half', color: 'half', strokeWidth: 2 }),\n  ],\n  scales: {\n    x: { scale: () => scalePoint().padding(0.02), axis: { tickLabels: { thin: false, rotate: -28 } } },\n    y: { scale: scaleLinear, nice: true, grid: true, axis: { ticks: { format: eurShort } } },\n  },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: {\n    use: tooltip,\n    format: (point) => point.datum.half + ' ' + point.datum.month_of_half + ': ' + eur(point.datum.cumulative_eur),\n  },\n});", "reasoning_plan": "the request is written in English, so language en\n'added up' is a running total, so an area, and 'compare' puts a second band beside it\nthe half is the series, so one band per half and a legend\nthe axis is the month inside the half, 1 to 6, so both bands start at the same place\nthe columns are month_of_half, half and cumulative_eur, the euro figure last", "reasoning_code": "the shape is area and these rows carry a series, so one areaY and one lineY over all of them\nthe columns are month_of_half, half and cumulative_eur, and cumulative_eur holds the numbers\nhalf names the series, so it is the z and the color channel on both marks and the legend goes on\nthe two bands are compared and not added, so each rests on zero itself: y1 zero, y2 the figure\nan area rests on zero, so its euro axis is the bare scaleLinear factory"}
{"id": "c18-couple-de-050", "household": "couple", "language": "de", "request": "Wie haben sich die Ausgaben auf dem Privatkonto von Jonas 2025 verteilt?", "shape": "doughnut", "difficulty": 1, "plan": {"title": "Privatkonto nach Kategorie", "question": "die sechs groessten Ausgabenkategorien des Jahres 2025 nur auf dem Privatkonto von Jonas mit ihren Summen, eine Zeile je Kategorie, Spalten topic und total_eur", "columns": ["topic", "total_eur"]}, "sql": "SELECT coalesce(category, 'Needs review') AS topic, ROUND(-SUM(amount), 2) AS total_eur\nFROM transaction_view\nWHERE amount < 0 AND account = 'Sparkasse Stuttgart Privatkonto Jonas'\nGROUP BY topic\nORDER BY total_eur DESC\nLIMIT 6", "code": "const short = (name) => (name.length > 18 ? name.slice(0, 17) + '.' : name);\nconst slices = pie(data, { value: 'total_eur' });\nreturn defineChart({\n  marks: [\n    polar({\n      inset: 6,\n      radiusRatio: 0.92,\n      marks: [\n        radialArc(slices, {\n          innerRadius: ({ radius }) => radius * 0.62,\n          color: (slice) => short(slice.topic),\n          key: 'topic',\n        }),\n      ],\n      scales: { angle: null, radius: null },\n    }),\n  ],\n  scales: { x: null, y: null },\n  color: { legend: colorLegend({ placement: 'bottom', itemWidth: 150 }) },\n  tooltip: { use: tooltip, format: (point) => point.datum.topic + ': ' + eur(point.datum.total_eur) },\n});", "reasoning_plan": "die Anfrage ist deutsch, also language de\nsie fragt, wie sich ein Konto aufteilt, das ist ein Anteil am Ganzen\nein Doughnut zeigt Anteile am direktesten\ndas Profil hat zwei Konten, gemeint ist nur das Privatkonto von Jonas\ndie Spalten sind topic und total_eur, der Eurowert zuletzt", "reasoning_code": "the shape is doughnut, so pie allocates the slices and radialArc draws them\nthe columns are topic and total_eur, and total_eur is the value pie reads\nthe slices are the series, so color and key both name topic, the column of names\na doughnut has no axes, so scales.x and scales.y are null and the legend is on"}
